# Sensitive folders/paths for this project.
#
# If a PR touches any path listed here, the "local-review" job ALWAYS
# triggers, regardless of diff size (see .github/workflows/ci.yml).
#
# Format: one path (or path prefix) per line, relative to the repo root.
# - Empty lines and lines starting with "#" are ignored.
# - The match is "contains this path as a prefix" (not a glob or regex),
#   so "auth/" matches "backend/auth/login.py" only if you write
#   "backend/auth/". Adjust the paths to the project's real structure
#   once the stack is decided.
#
# Edit this list to add/remove sensitive folders for the project.

auth/
payments/
security/
migrations/
